import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Mobile.App.Homepage.Wireframe',
  appName: 'Mobile App Homepage Wireframe',
  webDir: 'build'
};

export default config;
